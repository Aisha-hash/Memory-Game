'use strict';

const settings = {
    cards: [],
    elements: {},
    score: 0,
    elapsedTime: 0,
    level: 2
}

export default settings;
export const elements = settings.elements;
export const cards = settings.cards;