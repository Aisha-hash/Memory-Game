'use strict';

const settings = {
    cards: [],
    cardsLength: 0,
    elements: {},
    score: 0,
    elapsedTime: 0,
    level: 1,
    cardsWidth: 140,
    flipped: 0,
    numOfCards: 2,
    timerCounter: 0,
    previousScore: 0,
    seconds: 0,
    minutes: 0,
    dataCards: 'food'
}

export default settings;
export const elements = settings.elements;
export const cards = settings.cards;